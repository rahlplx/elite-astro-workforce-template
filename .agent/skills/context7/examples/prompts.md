# Elite Workforce Context7 Prompts

Ready-to-use prompts for fetching documentation relevant to the Elite Workforce project.

---

## Astro Framework

### Components

```markdown
How do I create a reusable Astro component with TypeScript props? use library /withastro/astro
```

```markdown
Show me Astro's component slot syntax for nested content. use library /withastro/astro
```

```markdown
How do I import and use components in Astro pages? use library /withastro/astro
```

### Layouts

```markdown
How do I create a base layout in Astro that wraps all pages? use library /withastro/astro
```

```markdown
How do I pass page-specific data to Astro layouts? use library /withastro/astro
```

### Static Site Generation

```markdown
How do I generate static pages from data in Astro? use library /withastro/astro
```

```markdown
How do I use getStaticPaths in Astro for dynamic routes? use library /withastro/astro
```

### Image Optimization

```markdown
How do I use the Image component in Astro for optimized images? use library /withastro/astro
```

```markdown
What image formats does Astro support for optimization? use library /withastro/astro
```

---

## Tailwind CSS

### Tailwind Configuration

```markdown
How do I extend the default Tailwind theme with custom colors? use library /tailwindlabs/tailwindcss
```

```markdown
How do I add custom fonts to Tailwind CSS config? use library /tailwindlabs/tailwindcss
```

```markdown
What's the correct syntax for custom animations in Tailwind? use library /tailwindlabs/tailwindcss
```

### Dark Mode

```markdown
How do I implement dark mode with class strategy in Tailwind 3.4? use library /tailwindlabs/tailwindcss
```

```markdown
How do I style elements differently in dark mode with Tailwind? use library /tailwindlabs/tailwindcss
```

### Responsive Design

```markdown
What are the default breakpoints in Tailwind CSS? use library /tailwindlabs/tailwindcss
```

```markdown
How do I create a responsive grid that changes columns at breakpoints? use library /tailwindlabs/tailwindcss
```

### Accessibility

```markdown
How do I hide elements visually but keep them for screen readers in Tailwind? use library /tailwindlabs/tailwindcss
```

```markdown
What's the focus-visible class in Tailwind and when should I use it? use library /tailwindlabs/tailwindcss
```

---

## TypeScript

### Interfaces

```markdown
How do I define optional properties in TypeScript interfaces? use library /microsoft/typescript
```

```markdown
How do I extend interfaces in TypeScript? use library /microsoft/typescript
```

### Types

```markdown
What's the difference between type and interface in TypeScript? use library /microsoft/typescript
```

```markdown
How do I create union types in TypeScript? use library /microsoft/typescript
```

### TypeScript Configuration

```markdown
What TypeScript compiler options are recommended for strict mode? use library /microsoft/typescript
```

---

## Accessibility (General)

```markdown
What are the WCAG 2.1 AA color contrast requirements? use context7
```

```markdown
How do I make a card component accessible with ARIA? use context7
```

```markdown
What's the minimum touch target size for WCAG compliance? use context7
```

```markdown
How do I implement skip navigation links for accessibility? use context7
```

---

## Performance

```markdown
How do I defer non-critical CSS in Astro? use library /withastro/astro
```

```markdown
What's the best way to lazy load images in Astro? use library /withastro/astro
```

```markdown
How do I inline critical CSS in Astro for better FCP? use context7
```

---

## Elite Workforce Specific

### Service Cards

```markdown
Create an accessible card component with hover animations, price badge, 
and gradient backgrounds using Astro and Tailwind. 
use library /withastro/astro use library /tailwindlabs/tailwindcss
```

### Team Section

```markdown
How do I create a responsive team member grid with profile images in Astro?
use library /withastro/astro
```

### Testimonials

```markdown
Create a testimonial carousel component with star ratings in Astro.
use library /withastro/astro
```

### Forms

```markdown
How do I create an accessible contact form in Astro without JavaScript?
use library /withastro/astro
```

---

### Combined Context Queries

Use multiple library tags for comprehensive answers:

```markdown
Create a premium service card with TypeScript props, Tailwind styling, 
dark mode support, and WCAG accessibility. 
use library /withastro/astro use library /tailwindlabs/tailwindcss
```

```markdown
Set up an Astro project with Tailwind CSS and TypeScript strict mode.
use library /withastro/astro use library /tailwindlabs/tailwindcss use library /microsoft/typescript
```
